package com.badals.admin.domain.enumeration;

/**
 * The ShipmentStatus enumeration.
 */
public enum ShipmentStatus {
    PENDING, IN_TRANSIT, RECEIVED, DELIVERED, CANCELED, FAILED,PROCESSING, SCHEDULED, ACCEPTED,CLOSED;
}
